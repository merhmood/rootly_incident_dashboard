version: '3.8'

services:
  rootly-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - ROOTLY_API_KEY=${ROOTLY_API_KEY}
    restart: always
